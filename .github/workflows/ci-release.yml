name: CI Release

on:
  workflow_call:
    secrets:
      PAGES_DEPLOY_TOKEN:
        required: true

jobs:
  release:
    name: GitHub Release
    runs-on: ubuntu-22.04

    permissions:
      contents: write

    steps:
      - name: Checkout source repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download DEB artifact
        uses: actions/download-artifact@v4
        with:
          name: siderust-cpp-deb
          path: dist/

      - name: Download RPM artifact
        uses: actions/download-artifact@v4
        with:
          name: siderust-cpp-rpm
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Checkout Pages repository
        uses: actions/checkout@v4
        with:
          repository: Siderust/siderust.github.io
          token: ${{ secrets.PAGES_DEPLOY_TOKEN }}
          path: pages
          ref: main

      - name: Install repository tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends dpkg-dev createrepo-c rpm

      - name: Update APT/RPM repositories
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p pages/apt
          mkdir -p pages/rpm

          cp dist/*.deb pages/apt/
          cp dist/*.rpm pages/rpm/

          cd pages/apt
          dpkg-scanpackages . /dev/null | gzip -9c > Packages.gz

          cd ../rpm
          createrepo_c .

      - name: Validate staged package metadata
        shell: bash
        run: scripts/validate-package-metadata.sh --package-dir dist

      - name: Debian bookworm install smoke test
        shell: bash
        run: scripts/smoke-install-debian-bookworm.sh --apt-repo-dir pages/apt --version 0.6.0

      - name: Commit package repository updates
        shell: bash
        run: |
          set -euo pipefail
          cd pages

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add apt rpm

          if git diff --cached --quiet; then
            echo "No package repository changes"
          else
            git commit -m "Update package repositories"
            git push origin main
          fi
